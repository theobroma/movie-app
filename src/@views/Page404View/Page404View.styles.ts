import { createStyles, makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() =>
  createStyles({
    mainTitle: {
      fontSize: '100px',
    },
    returnIcon: {
      marginRight: '10px',
    },
  }),
);
