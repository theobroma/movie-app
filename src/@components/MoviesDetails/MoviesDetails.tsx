import * as React from 'react';

interface Props {
  url?: any;
}

const MoviesDetails: React.FC<Props> = ({ url }) => {
  return (
    <div>
      <span>MoviesDetails</span>
    </div>
  );
};

export default MoviesDetails;
